{{/*
MLflow ServiceMonitor - Now provided by Bitnami MLflow subchart

Metrics are enabled via values.yaml:
  mlflow.tracking.metrics.enabled: true
  mlflow.tracking.metrics.serviceMonitor.enabled: true

The Bitnami chart creates its own ServiceMonitor when these options are set.
This file is kept for documentation purposes only.
*/}}
