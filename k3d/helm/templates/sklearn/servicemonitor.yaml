{{- if and .Values.sklearn (index .Values "kube-prometheus-stack" "enabled") }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-sklearn
  namespace: {{ .Release.Namespace }}
  labels:
    app: coelho-realtime-sklearn
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: coelho-realtime-sklearn
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
      scheme: http
{{- end }}
