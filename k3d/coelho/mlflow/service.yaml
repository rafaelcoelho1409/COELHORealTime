apiVersion: v1
kind: Service
metadata:
  name: coelho-realtime-mlflow-service
  namespace: coelho
  labels:
    app: coelho-realtime-mlflow-deployment
spec:
  selector:
    app: coelho-realtime-mlflow-deployment
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  type: LoadBalancer #ClusterIP
