# TODO

## Batch ML

- [ ] Add refresh button next to "MLflow Run" header in `batch_ml_run_and_training_box` component to manually refresh MLflow run IDs dropdown (applies to all batch pages: TFD, ETA, ECCI)
